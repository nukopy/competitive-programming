#include <iostream>
#include <vector>
#include <string>
using namespace std;
using ll = long long;

int main() {
    // input
    ll N, M;
    cin >> N >> M;
    vector<ll> vec(N);
    for (ll i = 0; i < N; i++) cin >> vec[i];
    
    // calculation
    
    
    return 0;
}