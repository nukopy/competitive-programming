#include <iostream>
#include <vector>
#include <string>
using namespace std;
using ll = long long;

int main() {
    // input
    ll N, M;
    cin >> N >> M;
    vector<ll> vec[M], A(N+1), B(M);
    for (ll i = 0; i < M; i++) cin >> vec[i];
    
    // calculation
    
    
    return 0;
}